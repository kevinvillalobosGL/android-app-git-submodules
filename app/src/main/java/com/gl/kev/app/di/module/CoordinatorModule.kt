package com.gl.kev.app.di.module

import android.app.Application
import dagger.Module


@Module
class CoordinatorModule(private val mApplication: Application) {

    /* @Provides
     @Singleton
     internal fun provideApiHelper(navigator: Navigator): Navigator {
         return navigator
     }

     @Provides
     @Singleton
     internal fun provideApiHelper(coordinator: RootFlowCoordinator): RootFlowCoordinator {
         return coordinator
     }*/


}